/**
 * @typedef {{ x: number, y: number }} ControlPoint

 * @typedef {{
 *   x: number,
 *   y: number,
 *   t: number,
 *   velocity: number,
 *   dy_dt: number,
 *   acc: number
 * }} CurveSample

 * @typedef {{ position: number, velocity: number, acceleration: number }} PhysicsMetrics

 * @typedef {{ id: string, name: string, value: [number,number,number,number] }} AnimationPreset
 */

export {};
